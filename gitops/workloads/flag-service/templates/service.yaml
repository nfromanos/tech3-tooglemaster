apiVersion: v1
kind: Service
metadata:
  name: {{ include "flag-service.fullname" . }}
  labels:
    {{- include "flag-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    {{- include "flag-service.selectorLabels" . | nindent 4 }}
