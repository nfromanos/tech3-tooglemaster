apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: targeting
spec:
  selector:
    app.kubernetes.io/name: targeting-service
  ports:
  - name: http
    protocol: TCP
    port: 8003
    targetPort: 8003